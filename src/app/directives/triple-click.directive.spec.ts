import { TripleClickDirective } from './triple-click.directive';

describe('TripleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TripleClickDirective();
    expect(directive).toBeTruthy();
  });
});
